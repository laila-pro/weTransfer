{% extends 'base.html.twig' %}
{% form_theme formTransfer 'bootstrap_4_layout.html.twig' %}
{% block title %}Files transfer{% endblock %}

{% block body %}
  <style>
    .styleform {
      margin: 1em auto;
      max-width: 600px;
      /* width: 65%; */
      font: 18px/1.5 sans-serif;
    }
    .styleform  {
      background: #F5F5F5;
      padding: 2px 6px;
    }
    .body {
      backgroud:#F4F4F4;
    }
  </style>
  <div class="container body">
    <div class="row styleform">
      <div class="col-8">
        <h1> We Transfer</h1>
        {{ form_start(formTransfer) }}
        {{ form_widget(formTransfer) }}
        {{ form_end(formTransfer) }}
      </div>
    </div>
  </div>
{% endblock %}
